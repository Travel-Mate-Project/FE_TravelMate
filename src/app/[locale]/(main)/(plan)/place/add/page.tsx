import React from 'react';

import {AddPlacePageProps} from '@/types';

export default function AddPlacePage({type}: AddPlacePageProps) {
  return (
    <div className="mx-auto w-full max-w-[600px]">
      <div>장소 추가 페이지</div>
    </div>
  );
}
